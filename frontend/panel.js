/**
 * panel.js — station panel helpers (M5 stub)
 */

/**
 * Render arrivals data into the dot-matrix board.
 * @param {Array} arrivals - TfL arrivals array from backend
 */
export function renderArrivals(arrivals) {
  // M5: Enhanced dot-matrix board rendering will be implemented here.
}

/**
 * Start the facts ticker scroll animation.
 * @param {string[]} facts - Array of fact sentences to scroll
 */
export function startFactsScroll(facts) {
  // M5: CSS keyframe or requestAnimationFrame scroll will be implemented here.
}
