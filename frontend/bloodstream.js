/**
 * bloodstream.js — M5 art layer stub
 *
 * The Victoria Line as a circulatory system.
 * Train positions will be rendered as amber boluses of light
 * moving through an artery.
 *
 * M4: stub only — canvas is sized and ready, animation loop is wired up.
 */

export function initBloodstream(map, canvas, getTrainState, getStationData) {
  // M5: Bloodstream animation will be implemented here.
  // The canvas is pointer-events:none (set in CSS) and sits above the Leaflet map.
  // Use map.latLngToContainerPoint() to convert geographic coordinates
  // to canvas pixel coordinates on each animation frame.
}
